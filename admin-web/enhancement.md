only existing student ID can register else this ID is not existing in the system.
if incorrect login credentials are provided, the page still redirects to the login page with error message invalid credentials. The user can try again to login with correct credentials.
the

---

- if account registration is rejected by dept head automatically delete the account in the database

---

Each announcement should support:
• Title (optional)
• Image upload (single or multiple)
• External link (any valid URL)
• Date and time posted
• Posted by (President or Department Head)
• Department category (optional)
